# 主流水线（根目录脚本）
pandas>=2.0
requests>=2.28
curl_cffi>=0.7  # 直连 SofaScore 官方接口（浏览器 TLS 指纹，绕过 Cloudflare）
penaltyblog>=1.0

# backtest/ 与校准图等
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3
